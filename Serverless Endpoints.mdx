---
title: 'Serverless Endpoints'
description: 'Access models via our serverless API with pay-per-token pricing. No infrastructure management required.'
---

<Info>
  **Prerequisites**:
  - A Valid Qubrid AI Account logged in the platform
  - Enough Credits in your Account should be present to make sure the requests get processed
  - A Valid API Ke to be replaced in ```YOUR_API_KEY``` section, generated using steps mentioned in [How to generate an API Key](Manage%20API%20Keys#how-to-generate-an-api-key)
</Info>

## Mistral 7B Instruct v0.3 - [View Model](https://platform.qubrid.com/model/mistralai-mistral-7b)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
  import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "mistralai/Mistral-7B-Instruct-v0.3",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# {"choices": [{"message": {"content": "Quantum computing uses qubits..."}}]}
```
```Javascript Javascript icon=java lines expandable theme={null}
  const url = 'https://platform.qubrid.com/api/v1/qubridai/chat/completions';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "mistralai/Mistral-7B-Instruct-v0.3",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// data.choices[0].message.content
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model": "mistralai/Mistral-7B-Instruct-v0.3",
		"messages": []map[string]string{
			{
				"role":    "user",
				"content": "Explain quantum computing simply.",
			},
		},
		"temperature": 0.7,
		"max_tokens":  500,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {"choices":[{"message":{"content":"Quantum computing uses qubits..."}}]}
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/chat/completions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "mistralai/Mistral-7B-Instruct-v0.3",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}'
  
# Expected Output:
# {
  "choices": [
    {"message": {"content": "Quantum computing uses qubits..."}}
  ]
}
```
</CodeGroup>

## GPT-OSS 20B - [View Model](https://platform.qubrid.com/model/openai-gpt-oss-20b)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "openai/gpt-oss-20b",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# {"choices": [{"message": {"content": "Quantum computing uses qubits..."}}]}
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/chat/completions';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "openai/gpt-oss-20b",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// data.choices[0].message.content
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model": "openai/gpt-oss-20b",
		"messages": []map[string]string{
			{
				"role":    "user",
				"content": "Explain quantum computing simply.",
			},
		},
		"temperature": 0.7,
		"max_tokens":  500,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {"choices":[{"message":{"content":"Quantum computing uses qubits..."}}]}
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/chat/completions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "openai/gpt-oss-20b",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}'
  
# Expected Output:
# {
  "choices": [
    {"message": {"content": "Quantum computing uses qubits..."}}
  ]
}
```
</CodeGroup>

## IBM Granite 20B Code Instruct - [View Model](https://platform.qubrid.com/model/ibm-granite-20b)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "ibm-granite/granite-20b-code-instruct-8k",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# {}
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/chat/completions';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "ibm-granite/granite-20b-code-instruct-8k",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// {}
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model": "ibm-granite/granite-20b-code-instruct-8k",
		"messages": []map[string]string{
			{
				"role":    "user",
				"content": "Explain quantum computing simply.",
			},
		},
		"temperature": 0.7,
		"max_tokens":  500,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {}
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/chat/completions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "ibm-granite/granite-20b-code-instruct-8k",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}'
  
# Expected Output:
# {}
```
</CodeGroup>

## Stable Code 3B - [View Model](https://platform.qubrid.com/model/stabilityai-stable-code)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "stabilityai/stable-code-instruct-3b",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# {}
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/chat/completions';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "stabilityai/stable-code-instruct-3b",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// {}
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/chat/completions"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model": "stabilityai/stable-code-instruct-3b",
		"messages": []map[string]string{
			{
				"role":    "user",
				"content": "Explain quantum computing simply.",
			},
		},
		"temperature": 0.7,
		"max_tokens":  500,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {}
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/chat/completions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "stabilityai/stable-code-instruct-3b",
  "messages": [
    {
      "role": "user",
      "content": "Explain quantum computing simply."
    }
  ],
  "temperature": 0.7,
  "max_tokens": 500
}'
  
# Expected Output:
# {}
```
</CodeGroup>

## Qwen Image Edit - [View Model](https://platform.qubrid.com/model/qwen-qwen2-vl-edit)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/image/generation"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "Qwen/Qwen-Image-Edit",
  "positive_prompt": "A futuristic city at sunset with flying cars",
  "width": 1024,
  "height": 1024,
  "steps": 30,
  "cfg": 7.5,
  "seed": 42
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# Binary PNG image stream (save with open('generated.png','wb').write(response.content))
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/image/generation';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "Qwen/Qwen-Image-Edit",
  "positive_prompt": "A futuristic city at sunset with flying cars",
  "width": 1024,
  "height": 1024,
  "steps": 30,
  "cfg": 7.5,
  "seed": 42
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// image binary (save as generated_image.png)
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/image/generation"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model":           "Qwen/Qwen-Image-Edit",
		"positive_prompt": "A futuristic city at sunset with flying cars",
		"width":           1024,
		"height":          1024,
		"steps":           30,
		"cfg":             7.5,
		"seed":            42,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// PNG binary stream
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/image/generation" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "Qwen/Qwen-Image-Edit",
  "positive_prompt": "A futuristic city at sunset with flying cars",
  "width": 1024,
  "height": 1024,
  "steps": 30,
  "cfg": 7.5,
  "seed": 42
}'
  
# Expected Output:
# PNG binary image stream with headers:
{
  "X-Cost-Info": "{\"total_cost\": 0.08}",
  "X-Prompt-ID": "abc123"
}
```
</CodeGroup>

## Stable Diffusion - [View Model](https://platform.qubrid.com/model/stability-ai-sd3.5-large)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/image/generation"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "stabilityai/stable-diffusion-3.5-large",
  "positive_prompt": "A futuristic city at sunset with flying cars",
  "width": 1024,
  "height": 1024,
  "steps": 30,
  "cfg": 7.5,
  "seed": 42
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# Binary PNG image stream (save with open('generated.png','wb').write(response.content))
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/image/generation';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "stabilityai/stable-diffusion-3.5-large",
  "positive_prompt": "A futuristic city at sunset with flying cars",
  "width": 1024,
  "height": 1024,
  "steps": 30,
  "cfg": 7.5,
  "seed": 42
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// image binary (save as generated_image.png)
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/image/generation"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model":           "stabilityai/stable-diffusion-3.5-large",
		"positive_prompt": "A futuristic city at sunset with flying cars",
		"width":           1024,
		"height":          1024,
		"steps":           30,
		"cfg":             7.5,
		"seed":            42,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// PNG binary stream
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/image/generation" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "stabilityai/stable-diffusion-3.5-large",
  "positive_prompt": "A futuristic city at sunset with flying cars",
  "width": 1024,
  "height": 1024,
  "steps": 30,
  "cfg": 7.5,
  "seed": 42
}'
  
# Expected Output:
# PNG binary image stream with headers:
{
  "X-Cost-Info": "{\"total_cost\": 0.08}",
  "X-Prompt-ID": "abc123"
}
```
</CodeGroup>

## Whisper Large V3 - [View Model](https://platform.qubrid.com/model/whisper-large-v3)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/audio/transcribe"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

files = {"file": open("audio.wav", "rb")}
data = {"model": "openai/whisper-large-v3"}
response = requests.post(url, headers=headers, files=files, data=data)
print(response.json())

# Expected Output:
# {"text": "Welcome to Qubrid AI", "language": "en"}
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/audio/transcribe';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const form = new FormData();
form.append('file', fs.createReadStream('audio.wav'));
form.append('model', 'openai/whisper-large-v3');

const res = await fetch(url, { method: 'POST', headers, body: form });
console.log(await res.json());

# Expected Output:
// { text: "Welcome to Qubrid AI" }
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"fmt"
	"io"
	"mime/multipart"
	"net/http"
	"os"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/audio/transcribe"
	apiKey := "YOUR_API_KEY"

	body := &bytes.Buffer{}
	writer := multipart.NewWriter(body)

	writer.WriteField("model", "openai/whisper-large-v3")

	file, _ := os.Open("audio.wav")
	defer file.Close()
	part, _ := writer.CreateFormFile("file", "audio.wav")
	io.Copy(part, file)

	writer.Close()

	req, _ := http.NewRequest("POST", url, body)
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", writer.FormDataContentType())

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {"text": "Welcome to Qubrid AI"}
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/audio/transcribe" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F "model=openai/whisper-large-v3" \
  -F "file=@audio.wav"
  
# Expected Output:
# {
  "text": "Welcome to Qubrid AI.",
  "language": "en"
}
```
</CodeGroup>

## Qwen3-VL 8B Instruct - [View Model](https://platform.qubrid.com/model/qwen-qwen3-vl-instruct)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/multimodal/chat"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "Qwen/Qwen3-VL-8B-Instruct",
  "prompt": "Explain quantum computing simply.",
  "temperature": 0.7,
  "max_tokens": 500
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# {"choices": [{"message": {"content": "A brown cat sitting on a chair."}}]}
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/multimodal/chat';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "Qwen/Qwen3-VL-8B-Instruct",
  "prompt": "Explain quantum computing simply.",
  "temperature": 0.7,
  "max_tokens": 500
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// { choices: [{ message: { content: "Cat on chair" } }] }
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/multimodal/chat"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model":       "Qwen/Qwen3-VL-8B-Instruct",
		"prompt":      "Explain quantum computing simply.",
		"temperature": 0.7,
		"max_tokens":  500,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {"choices": [{"message": {"content": "This image shows..."}}]}
```
```Shell Shell icon=terminal expandable theme={null}
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/multimodal/chat" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "Qwen/Qwen3-VL-8B-Instruct",
  "prompt": "Explain quantum computing simply.",
  "temperature": 0.7,
  "max_tokens": 500
}'
  
# Expected Output:
# {
  "choices": [
    {"message": {"content": "This image shows a brown cat."}}
  ]
}
```
</CodeGroup>

## Qwen2.5-VL 7B instruct - [View Model](https://platform.qubrid.com/model/qwen-qwen2.5-vl-instruct)

<CodeGroup>
```Python Python icon=python lines expandable theme={null}
import requests, json, base64

url = "https://platform.qubrid.com/api/v1/qubridai/multimodal/chat"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

headers["Content-Type"] = "application/json"
data = {
  "model": "Qwen/Qwen2.5-VL-7B-Instruct",
  "prompt": "Explain quantum computing simply.",
  "temperature": 0.7,
  "max_tokens": 500
}
response = requests.post(url, headers=headers, json=data)
print(json.dumps(response.json(), indent=2))

# Expected Output:
# {"choices": [{"message": {"content": "A brown cat sitting on a chair."}}]}
```
```Javascript Javascript icon=java lines expandable theme={null}
const url = 'https://platform.qubrid.com/api/v1/qubridai/multimodal/chat';
const headers = { 'Authorization': 'Bearer YOUR_API_KEY' };

const body = {
  "model": "Qwen/Qwen2.5-VL-7B-Instruct",
  "prompt": "Explain quantum computing simply.",
  "temperature": 0.7,
  "max_tokens": 500
};
headers['Content-Type'] = 'application/json';
const res = await fetch(url, { method: 'POST', headers, body: JSON.stringify(body) });
console.log(await res.json());

# Expected Output:
// { choices: [{ message: { content: "Cat on chair" } }] }
```
```Go Go icon=golang lines expandable theme={null}
package main

import (
	"bufio"
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://platform.qubrid.com/api/v1/qubridai/multimodal/chat"
	apiKey := "YOUR_API_KEY"

	data := map[string]interface{}{
		"model":       "Qwen/Qwen2.5-VL-7B-Instruct",
		"prompt":      "Explain quantum computing simply.",
		"temperature": 0.7,
		"max_tokens":  500,
	}

	jsonData, _ := json.Marshal(data)
	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
	req.Header.Set("Authorization", "Bearer "+apiKey)
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	res, _ := client.Do(req)
	defer res.Body.Close()

	scanner := bufio.NewScanner(res.Body)
	for scanner.Scan() {
		line := scanner.Text()
		if line != "" {
			fmt.Println(line)
		}
	}
}

// Expected Output:
// {"choices": [{"message": {"content": "This image shows..."}}]}
```
```Shell Shell icon=terminal expandable theme={null}
# Example cURL for multimodal
curl -X POST "https://platform.qubrid.com/api/v1/qubridai/multimodal/chat" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "Qwen/Qwen2.5-VL-7B-Instruct",
  "prompt": "Explain quantum computing simply.",
  "temperature": 0.7,
  "max_tokens": 500
}'
  
# Expected Output:
# {
  "choices": [
    {"message": {"content": "This image shows a brown cat."}}
  ]
}
```
</CodeGroup>